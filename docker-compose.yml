services:
  grok2api:
    build: .
    container_name: grok2api
    ports:
      - "8000:8000"
    environment:
      TZ: Asia/Shanghai
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: 8000
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
